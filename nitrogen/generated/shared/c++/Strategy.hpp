///
/// Strategy.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nearbyconnections {

  /**
   * An enum which can be represented as a JavaScript enum (Strategy).
   */
  enum class Strategy {
    P2P_CLUSTER      SWIFT_NAME(p2pCluster) = 1,
    P2P_STAR      SWIFT_NAME(p2pStar) = 2,
    P2P_POINT_TO_POINT      SWIFT_NAME(p2pPointToPoint) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nearbyconnections

namespace margelo::nitro {

  // C++ Strategy <> JS Strategy (enum)
  template <>
  struct JSIConverter<margelo::nitro::nearbyconnections::Strategy> final {
    static inline margelo::nitro::nearbyconnections::Strategy fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<margelo::nitro::nearbyconnections::Strategy>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nearbyconnections::Strategy arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime&, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double number = value.getNumber();
      int integer = static_cast<int>(number);
      if (number != integer) {
        // The integer is not the same value as the double - we truncated floating points.
        // Enums are all integers, so the input floating point number is obviously invalid.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 1 && integer <= 3;
    }
  };

} // namespace margelo::nitro
